// The cover wordmark on its own: the two-line ransom masthead, no sheet, no
// paper. Exported as a transparent PNG (omitBackground) so it can drop onto any
// ground — slides, tote, sticker. Prerendered like every other /print route.
export const prerender = true;
